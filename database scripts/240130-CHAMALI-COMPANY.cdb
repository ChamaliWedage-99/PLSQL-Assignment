CREATE TABLE chamali_company_tab(
       company_reg_no NUMBER GENERATED BY DEFAULT AS IDENTITY,
       company_name VARCHAR2(200),
       company_address VARCHAR2(4000),
       PRIMARY KEY(company_reg_no)
);